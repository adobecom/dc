/**
 * @jest-environment jsdom
 */
const { ccTranslations } = import('../../acrobat/scripts/ccTranslations.js');
/**  import { readFile } from '@web/test-runner-commands';
import { expect } from '@esm-bundle/chai';
import sinon from 'sinon';
// import { ccTranslations } from '../../acrobat/scripts/ccTranslations.js';

describe('CC Placeholder Image', async () => {
  it('should find data attritubes', async () => {
    const dataAttribute = 'data-local';
    const ccAttr = 'credit-cards';
    // check the mock for the data attribute
  });

  it('should check if the image exsists', async () => {
    // call the function that checks the image
  });

  it('should log if image path failed', async () => {
    // call the lana function
  });

  it("should check if image is on page", async () => {
  // do a search of the image in the mock html
  });

  afterEach(() => {
    window.lana.log.resetHistory();
  });
});
*/
