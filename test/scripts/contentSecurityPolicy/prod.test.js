const {
  connectSrc,
  defaultSrc,
  fontSrc,
  formAction,
  frameSrc,
  imgSrc,
  manifestSrc,
  scriptSrc,
  styleSrc,
  workerSrc,
} = await import('../../../acrobat/scripts/contentSecurityPolicy/prod');
