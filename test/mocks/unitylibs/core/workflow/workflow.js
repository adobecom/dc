/* global jest */
/* eslint-disable no-unused-vars */
export default jest.fn(async (el, project = 'unity', unityLibs = '/unitylibs', unityVersion = 'v1', langRegion = 'us', langCode = 'en') => {
  // Mock implementation - can be customized as needed
}); 